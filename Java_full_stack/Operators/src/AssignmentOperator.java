public class AssignmentOperator {
	public static void main(String[] args) {
		// So here we are going to take a look at = operator along with few of its variations
		int x = 10; // This is basic assignment of value 10 to integer variable x.
		// Now lets have a look at the variabtions as well
		// +=, -=, *=, /=, %=
	}
}
